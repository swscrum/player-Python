from models.game_state import GameState
from models.player_action import PlayerAction


def decide(gameState: GameState) -> PlayerAction:
    # TODO: place your logic here
    return PlayerAction(0, 0, 0)
